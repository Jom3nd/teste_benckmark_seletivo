# Python project
Requer Python 3.11+. `python -m pip install -r requirements.txt`; `pytest`; analisador: `python tools/analyzer.py --mode benchmark --repeat 5`; escala: `python tools/generate_fixture.py --modules 100`.
